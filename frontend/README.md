# Keystone AI Frontend

React and Vite power the single-session Keystone study scaffold flow.

## Environment

Copy `.env.example` to `.env.local` if the backend is not running at the default URL.

```bash
VITE_API_URL=http://localhost:8000
```

## Scripts

```bash
npm run dev
npm run lint
npm run build
```
